name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # TODO: Implement full CI pipeline
      # - Install pnpm
      # - Install dependencies
      # - Gitleaks / Secret scanning (§19)
      # - ESLint check
      # - Build (Turbo)
      # - Run Vitest (unit tests)
      # - Run Playwright (e2e tests)

      - name: Placeholder test
        run: echo "CI pipeline to be implemented"
