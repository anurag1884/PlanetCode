// ============================================================
// PlanetCode — Planet Detail Page (Dashboard View)
// File: apps/web/src/app/(dashboard)/planets/[planetId]/page.tsx
// Status: PLACEHOLDER — not yet implemented
// ============================================================

// TODO: Implement planet detail/management page with:
// - Planet info (name, code, description)
// - Member list with remove capability (owner only)
// - Pending access requests (owner only)
// - "Enter Planet" button → password prompt → redirect to editor
// - Planet settings (owner only): rename, change password, delete

export default function PlanetDetailPage() {
  return (
    <div>
      <h1>Planet Details</h1>
      <p>Planet management UI will be rendered here.</p>
    </div>
  );
}
